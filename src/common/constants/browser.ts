/**
 * Default configuration for browser frame sessions.
 */
export const BROWSER_SESSION_DEFAULTS = {
  /** Maximum time (ms) a browser session bridge token is valid. */
  TOKEN_TTL_MS: 30_000,
} as const;
